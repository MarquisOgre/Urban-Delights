import { useMemo, useState } from "react";
import { ArrowRight, Check, Leaf, Minus, Plus, ShieldCheck, ShoppingBag, Sparkles, Trash2, Truck } from "lucide-react";
import { useQuery } from "@tanstack/react-query";
import { useNavigate } from "react-router-dom";
import { Button } from "@/components/ui/button";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Sheet, SheetContent, SheetDescription, SheetHeader, SheetTitle, SheetTrigger } from "@/components/ui/sheet";
import Footer from "@/components/Footer";
import { fetchRecipePricing, type RecipePricing } from "@/services/pricingService";
import { useStoreSettings, type StoreBenefit, type StoreSettings, type StoreWhyUs } from "@/hooks/useStoreSettings";
import chickenMasala from "@/assets/chicken-masala.jpg";
import garamMasala from "@/assets/garam-masala.jpg";
import sambarPodi from "@/assets/sambar-powder.jpg";
import rasamPodi from "@/assets/rasam-powder.jpg";
import karvepakuPodi from "@/assets/karvepaku-podi.jpg";
import kobariPodi from "@/assets/kobari-powder.jpg";
import palliPodi from "@/assets/palli-podi.jpg";
import putnaluPodi from "@/assets/putnalu-podi.jpg";
import idlyPodi from "@/assets/idly-podi.jpg";

type Product = { name: string; image: string; description: string; aliases: string[] };
type CartItem = { key: string; product: Product; pack: string; price: number; quantity: number };
type Filter = "All" | string;

const products: Product[] = [
  { name: "Chicken Masala", image: chickenMasala, description: "A robust, aromatic blend for deeply flavoured chicken dishes.", aliases: ["chicken masala"] },
  { name: "Garam Masala", image: garamMasala, description: "A warming finishing spice with whole-spice depth and aroma.", aliases: ["garam masala"] },
  { name: "Sambar Podi", image: sambarPodi, description: "A balanced lentil and spice blend for homestyle sambar.", aliases: ["sambar podi", "sambar powder"] },
  { name: "Rasam Podi", image: rasamPodi, description: "Peppery, tangy and fragrant for a comforting bowl of rasam.", aliases: ["rasam podi", "rasam powder"] },
  { name: "Karvepaku Podi", image: karvepakuPodi, description: "Earthy curry leaves ground with lentils and traditional spices.", aliases: ["karvepaku podi", "curry leaf"] },
  { name: "Kobari Podi", image: kobariPodi, description: "A savoury coconut blend with a gently roasted finish.", aliases: ["kobari podi", "kobari powder", "coconut"] },
  { name: "Palli Podi", image: palliPodi, description: "Roasted peanut podi with a satisfying nutty flavour.", aliases: ["palli podi", "peanut"] },
  { name: "Putnalu Podi", image: putnaluPodi, description: "Classic roasted gram podi, simple and full of flavour.", aliases: ["putnalu podi", "roasted gram"] },
  { name: "Idly Podi", image: idlyPodi, description: "The everyday South Indian companion for idli and dosa.", aliases: ["idly podi", "idli podi"] },
];
const normalize = (value: string) => value.toLowerCase().replace(/[^a-z0-9]/g, "");
const findPricing = (product: Product, pricing: RecipePricing[]) => pricing.filter((entry) => entry.is_enabled && product.aliases.some((alias) => normalize(entry.recipe_name).includes(normalize(alias))));
const findProduct = (name: string) => products.find((product) => normalize(product.name) === normalize(name) || product.aliases.some((alias) => normalize(name).includes(normalize(alias))));
const iconForBenefit = (icon: StoreBenefit["icon"]) => icon === "truck" ? Truck : icon === "shield" ? ShieldCheck : icon === "leaf" ? Leaf : ShoppingBag;
const iconForWhy = (icon: StoreWhyUs["icon"]) => icon === "shield" ? ShieldCheck : icon === "sparkles" ? Sparkles : icon === "check" ? Check : ShoppingBag;
const themeClass = (theme: string) => ({ orange: "bg-orange-50 border-orange-200", green: "bg-emerald-50 border-emerald-200", gold: "bg-amber-50 border-amber-200", rose: "bg-rose-50 border-rose-200", plum: "bg-purple-50 border-purple-200" }[theme] ?? "bg-stone-50 border-stone-200");

const Store = () => {
  const navigate = useNavigate();
  const { data: settings = {} as StoreSettings } = useStoreSettings();
  const { data: pricing = [], isLoading: pricingLoading } = useQuery({ queryKey: ["store-pricing"], queryFn: fetchRecipePricing });
  const [filter, setFilter] = useState<Filter>("All");
  const [selectedPacks, setSelectedPacks] = useState<Record<string, string>>({});
  const [cart, setCart] = useState<CartItem[]>([]);
  const [cartOpen, setCartOpen] = useState(false);
  const productImages = useMemo(() => new Map((settings.productMedia ?? []).map((item) => [normalize(item.name), item.imageUrl]).filter((item) => item[1])), [settings.productMedia]);
  const getImage = (product: Product) => productImages.get(normalize(product.name)) || product.image;
  const productPricing = useMemo(() => new Map(products.map((product) => [product.name, findPricing(product, pricing)])), [pricing]);
  const featuredProducts = useMemo(() => settings.featuredProductNames.map(findProduct).filter(Boolean) as Product[], [settings.featuredProductNames]);
  const filteredProducts = useMemo(() => { if (filter === "All") return products; const category = settings.categories.find((item) => item.title === filter); if (!category) return products; const allowed = new Set(category.productNames.map(normalize)); return products.filter((product) => allowed.has(normalize(product.name)) || product.aliases.some((alias) => allowed.has(normalize(alias)))); }, [filter, settings.categories]);
  const getPack = (product: Product) => { const entries = productPricing.get(product.name) ?? []; return selectedPacks[product.name] || entries[0]?.quantity_type || "100 g"; };
  const getPrice = (product: Product) => { const pack = getPack(product); return (productPricing.get(product.name) ?? []).find((entry) => entry.quantity_type === pack)?.price ?? 0; };
  const itemCount = cart.reduce((sum, item) => sum + item.quantity, 0);
  const subtotal = cart.reduce((sum, item) => sum + item.price * item.quantity, 0);
  const shipping = subtotal === 0 || subtotal >= settings.freeShippingAbove ? 0 : settings.shippingFee;
  const total = subtotal + shipping;
  const scrollTo = (id: string) => document.getElementById(id)?.scrollIntoView({ behavior: "smooth", block: "start" });
  const addToCart = (product: Product) => { const price = getPrice(product); const pack = getPack(product); if (price <= 0) return; const key = `${product.name}-${pack}`; setCart((current) => { const existing = current.find((item) => item.key === key); if (existing) return current.map((item) => item.key === key ? { ...item, quantity: item.quantity + 1 } : item); return [...current, { key, product, pack, price, quantity: 1 }]; }); setCartOpen(true); };
  const updateQuantity = (key: string, delta: number) => setCart((current) => current.map((item) => item.key === key ? { ...item, quantity: item.quantity + delta } : item).filter((item) => item.quantity > 0));
  const openCheckout = () => { if (!cart.length) return; sessionStorage.setItem("urban-delights-checkout-cart", JSON.stringify(cart.map((item) => ({ key: item.key, productName: item.product.name, pack: item.pack, price: item.price, quantity: item.quantity, image: getImage(item.product) })))); setCartOpen(false); navigate("/checkout"); };
  const ProductCard = ({ product }: { product: Product }) => { const entries = productPricing.get(product.name) ?? []; const pack = getPack(product); const price = getPrice(product); const image = getImage(product); return <article className="group overflow-hidden rounded-3xl border border-stone-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><div className="aspect-[4/3] overflow-hidden bg-stone-100"><img src={image} alt={product.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-105" onError={(event) => { event.currentTarget.src = product.image; }} /></div><div className="p-4 sm:p-5"><h3 className="font-bold text-stone-900">{product.name}</h3><p className="mt-1 min-h-10 text-sm leading-5 text-stone-500">{product.description}</p><div className="mt-4 flex items-center gap-2">{entries.length > 0 ? <Select value={pack} onValueChange={(value) => setSelectedPacks((current) => ({ ...current, [product.name]: value }))}><SelectTrigger className="h-10 flex-1"><SelectValue /></SelectTrigger><SelectContent>{entries.map((entry) => <SelectItem key={entry.quantity_type} value={entry.quantity_type}>{entry.quantity_type} · ₹{entry.price.toFixed(0)}</SelectItem>)}</SelectContent></Select> : <div className="flex-1 rounded-md bg-stone-50 px-3 py-2 text-xs text-stone-500">Price not configured</div>}<Button disabled={pricingLoading || price <= 0} onClick={() => addToCart(product)} className="h-10 rounded-xl bg-stone-900 px-4 hover:bg-orange-700">Add</Button></div>{price > 0 && <div className="mt-2 text-sm font-bold text-orange-700">₹{price.toFixed(0)}</div>}</div></article>; };
  return <div className="min-h-screen bg-stone-50 text-stone-900"><header className="sticky top-0 z-40 border-b border-stone-200 bg-white/95 backdrop-blur"><div className="container flex h-[72px] items-center justify-between gap-4 px-4 sm:px-6"><button onClick={() => scrollTo("top")} className="flex items-center gap-3" aria-label={`${settings.storeName} home`}><img src={settings.logoUrl || "/logo.png"} alt={settings.storeName} className="h-10 w-auto sm:h-12 object-contain" /><span className="hidden font-black tracking-tight text-stone-900 md:block">{settings.storeName}</span></button><nav className="hidden items-center gap-6 sm:flex"><button onClick={() => scrollTo("products")} className="text-sm font-semibold text-stone-600 hover:text-orange-700">Shop</button>{settings.showCategories && settings.categories.length > 0 && <button onClick={() => scrollTo("categories")} className="text-sm font-semibold text-stone-600 hover:text-orange-700">Categories</button>}{settings.showWhyUs && <button onClick={() => scrollTo("why-us")} className="text-sm font-semibold text-stone-600 hover:text-orange-700">Why Us</button>}</nav><div className="flex items-center gap-2">{settings.showAdminLogin && <Button variant="outline" size="icon" onClick={() => navigate("/login")} className="h-10 w-10 rounded-full" aria-label="Admin Login" title="Admin Login"><span className="sr-only">Admin Login</span><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2" /><circle cx="12" cy="7" r="4" /></svg></Button>}{settings.showBasket && <Sheet open={cartOpen} onOpenChange={setCartOpen}><SheetTrigger asChild><Button className="relative rounded-full bg-stone-900 px-4 text-white hover:bg-orange-700"><ShoppingBag className="mr-2 h-4 w-4" /> Basket <span className="ml-2 rounded-full bg-white/15 px-2 py-0.5 text-xs">{itemCount}</span></Button></SheetTrigger><SheetContent className="flex w-full flex-col bg-white sm:max-w-md"><SheetHeader><SheetTitle>Your basket</SheetTitle><SheetDescription>{itemCount ? `${itemCount} item${itemCount === 1 ? "" : "s"} selected` : "Your basket is ready for something delicious."}</SheetDescription></SheetHeader><div className="mt-5 flex-1 space-y-4 overflow-y-auto">{cart.length === 0 && <div className="border-y border-stone-200 py-10 text-center text-sm text-stone-500">Your basket is empty.</div>}{cart.map((item) => <div key={item.key} className="grid grid-cols-[56px_1fr_auto] gap-3 border-b border-stone-200 pb-4"><img src={getImage(item.product)} alt={item.product.name} className="h-14 w-14 rounded-lg object-cover" onError={(event) => { event.currentTarget.src = item.product.image; }} /><div><p className="font-semibold">{item.product.name}</p><p className="text-xs text-stone-500">{item.pack} · ₹{item.price.toFixed(0)}</p><div className="mt-2 flex items-center gap-1"><Button variant="outline" size="icon" className="h-7 w-7" onClick={() => updateQuantity(item.key, -1)}><Minus className="h-3 w-3" /></Button><span className="w-7 text-center text-sm font-semibold">{item.quantity}</span><Button variant="outline" size="icon" className="h-7 w-7" onClick={() => updateQuantity(item.key, 1)}><Plus className="h-3 w-3" /></Button></div></div><Button variant="ghost" size="icon" className="text-destructive" onClick={() => setCart((current) => current.filter((entry) => entry.key !== item.key))}><Trash2 className="h-4 w-4" /></Button></div>)}</div><div className="border-t border-stone-200 pt-5"><div className="flex items-center justify-between text-sm"><span>Subtotal</span><span>₹{subtotal.toFixed(0)}</span></div><div className="mt-2 flex items-center justify-between text-sm"><span>Shipping</span><span>{shipping === 0 ? "FREE" : `₹${shipping.toFixed(0)}`}</span></div><div className="my-4 flex items-center justify-between text-lg font-black"><span>Total</span><span>₹{total.toFixed(0)}</span></div><Button className="w-full bg-stone-900 hover:bg-orange-700" disabled={!cart.length} onClick={openCheckout}>Proceed to order</Button></div></SheetContent></Sheet>}</div></div></header><main id="top">{settings.showHero && <section className="relative overflow-hidden border-b border-stone-200 bg-gradient-to-br from-orange-50 via-amber-50 to-stone-100"><div className="absolute -right-24 -top-24 h-72 w-72 rounded-full bg-orange-200/30 blur-3xl" /><div className="container relative grid min-h-[520px] items-center gap-10 px-4 py-14 sm:px-6 sm:py-20 lg:grid-cols-[1.05fr_.95fr] lg:py-24"><div className="max-w-2xl"><div className="mb-5 inline-flex items-center gap-2 rounded-full border border-orange-200 bg-white/70 px-4 py-2 text-xs font-bold uppercase tracking-[0.18em] text-orange-800"><Sparkles className="h-3.5 w-3.5" /> {settings.heroEyebrow}</div><h1 className="text-4xl font-bold leading-[1.05] tracking-tight text-stone-950 sm:text-6xl lg:text-7xl">{settings.heroTitle} <span className="text-orange-700">{settings.heroTitleAccent}</span></h1><p className="mt-6 max-w-xl text-base leading-7 text-stone-600 sm:text-lg">{settings.heroDescription}</p><div className="mt-8 flex flex-wrap gap-3"><Button size="lg" onClick={() => scrollTo("products")} className="rounded-full bg-stone-900 px-7 text-white hover:bg-orange-700">{settings.heroPrimaryButtonText} <ArrowRight className="ml-2 h-4 w-4" /></Button><Button variant="outline" size="lg" onClick={() => scrollTo("why-us")} className="rounded-full border-stone-300 bg-white/60">{settings.heroSecondaryButtonText}</Button></div></div><div className="relative mx-auto w-full max-w-xl"><div className="absolute inset-5 rounded-[2rem] bg-orange-900/10 blur-2xl" /><div className="relative overflow-hidden rounded-[2rem] border border-white/80 bg-white/50 p-3 shadow-2xl backdrop-blur-sm sm:p-4"><img src={settings.heroImageUrl || chickenMasala} alt="" className="h-[300px] w-full rounded-[1.5rem] object-cover sm:h-[400px]" onError={(event) => { event.currentTarget.src = chickenMasala; }} /></div></div></div></section>}{settings.showBenefits && <section className="border-b border-stone-200 bg-white"><div className="container grid grid-cols-2 gap-3 px-4 py-7 sm:grid-cols-4 sm:px-6">{settings.benefits.map((benefit, index) => { const Icon = iconForBenefit(benefit.icon); return <div key={`${benefit.title}-${index}`} className="rounded-2xl border border-stone-100 p-4"><Icon className="h-5 w-5 text-orange-700" /><p className="mt-2 font-bold">{benefit.title}</p><p className="text-xs text-stone-500">{benefit.subtitle}</p></div>; })}</div></section>}{settings.showCategories && <section id="categories" className="container px-4 py-14 sm:px-6"><div className="flex items-end justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-widest text-orange-700">Browse</p><h2 className="mt-1 text-3xl font-black">Shop by category</h2></div><Button variant="outline" onClick={() => { setFilter("All"); scrollTo("products"); }}>All products</Button></div><div className="mt-7 grid gap-4 md:grid-cols-2">{settings.categories.map((category, index) => <button key={`${category.title}-${index}`} onClick={() => { setFilter(category.title); scrollTo("products"); }} className={`group overflow-hidden rounded-3xl border text-left ${themeClass(category.theme)}`}><div className="grid min-h-[180px] grid-cols-[1fr_140px] items-center gap-4 p-6"><div><h3 className="text-2xl font-black">{category.title}</h3><p className="mt-2 text-sm text-stone-600">{category.subtitle}</p><span className="mt-5 inline-flex text-sm font-bold text-orange-800">Explore <ArrowRight className="ml-2 h-4 w-4" /></span></div>{category.imageUrl && <img src={category.imageUrl} alt="" className="h-28 w-28 rounded-2xl object-cover shadow-sm" onError={(event) => { event.currentTarget.style.display = "none"; }} />}</div></button>)}</div></section>}{settings.showFeatured && featuredProducts.length > 0 && <section className="bg-white py-14"><div className="container px-4 sm:px-6"><div><p className="text-sm font-bold uppercase tracking-widest text-orange-700">Editor’s pick</p><h2 className="mt-1 text-3xl font-black">Featured products</h2></div><div className="mt-7 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{featuredProducts.map((product) => <ProductCard key={`featured-${product.name}`} product={product} />)}</div></div></section>}<section id="products" className="container scroll-mt-24 px-4 py-14 sm:px-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-bold uppercase tracking-widest text-orange-700">The collection</p><h2 className="mt-1 text-3xl font-black">All products</h2></div>{allCategoryNames.length > 0 && <div className="flex flex-wrap gap-2"><Button size="sm" variant={filter === "All" ? "default" : "outline"} onClick={() => setFilter("All")}>All</Button>{allCategoryNames.map((name) => <Button key={name} size="sm" variant={filter === name ? "default" : "outline"} onClick={() => setFilter(name)}>{name}</Button>)}</div>}</div><div className="mt-7 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{filteredProducts.map((product) => <ProductCard key={product.name} product={product} />)}</div></section>{settings.showWhyUs && <section id="why-us" className="scroll-mt-24 border-y border-stone-200 bg-white py-14"><div className="container px-4 sm:px-6"><p className="text-sm font-bold uppercase tracking-widest text-orange-700">Why us</p><h2 className="mt-1 text-3xl font-black">Made with care, served with pride.</h2><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{settings.whyUs.map((item, index) => { const Icon = iconForWhy(item.icon); return <div key={`${item.title}-${index}`} className={`rounded-3xl border p-6 ${themeClass(item.theme)}`}><Icon className="h-6 w-6 text-orange-700" /><h3 className="mt-4 font-black">{item.title}</h3><p className="mt-2 text-sm leading-6 text-stone-600">{item.description}</p></div>; })}</div></div></section>}</main><Footer showTopButton={true} /></div>;
};

export default Store;
